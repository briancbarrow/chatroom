{
  "name": "chatroom",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "mkdir": "mkdir build",
    "build": "npm run clean && npm run mkdir && npm run build:html && npm run build:css && npm run build:js",
    "clean": "rm -rf build",
    "build:html": "npm run clean:html && cp index.html build/",
    "clean:html": "rm -f build/index.html",
    "build:js": "npm run clean:js",
    "clean:js": "rm -f build/chatroom.1.0.0.js build/chatroom.1.0.0.js.map",
    "build:css": "npm run clean:css && lessc --source-map styles/main.less build/meetup-finder.css",
    "clean:css": "rm -f build/meetup-finder.css build/meetup-finder.css.map",
  },
  "author": "Brian Barrow",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^3.3.7",
    "express": "^4.14.0",
    "jquery": "^3.1.1",
    "socket.io": "^1.5.1"
  },
  "devDependencies": {
    "less": "^2.7.1"
  }
}
